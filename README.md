# GODADDY-UPLOAD-masterbuild-website-LIVE.zip

Complete standalone website (Pass 4). Extract into `public_html`.

~10 MB. Full site including `.htaccess` so folder URLs (`/field-tools/`, `/contact/`, `/start/`) work.

New in this package:
- `/start/` — problem router
- `/que-enviar/` — Spanish what-to-send
- `/preguntas-frecuentes/` — Spanish FAQ
- Stronger architect / owner / contractor briefs
- DirectoryIndex + MultiViews fix in `.htaccess`

## Extract (overwrite — no wipe required if the last full zip already landed)

1. Download **this file** (download icon). Not Code → Download ZIP.
2. `public_html` → Show Hidden Files → Overwrite existing files.
3. Upload. Extract into `public_html`. Wait until it finishes.
4. If a `GODADDY-UPLOAD-...` folder appears, move contents up.
5. Confirm `.htaccess`, `index.html`, `start`, `field-tools`, `contact` are in `public_html`.
6. Delete the zip.
7. Visit https://masterbuildconsulting.com/start/ and https://masterbuildconsulting.com/field-tools/
